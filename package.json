{
  "name": "lead-router",
  "version": "1.0.0",
  "description": "Inbound lead routing system — assigns leads to advisors and returns booking links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
